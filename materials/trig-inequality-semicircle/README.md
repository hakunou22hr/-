# 三角比の不等式を半円で考える

`index.html` をブラウザで開くか、リポジトリのルートで `npm run dev` を実行して `/materials/trig-inequality-semicircle/` にアクセスしてください。

0° から 180° の半円上で、sin・cos・tan の不等式を弧と関数グラフの両方に表示します。tan では 90° を定義域から除外し、開点・閉点で厳密な端点を表します。外部 API や外部アセットは使用していません。
