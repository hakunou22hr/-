# PDF Japanese font

Place a licensed, embeddable TrueType/OpenType Japanese font here as `NotoSansJP-Regular.ttf`.
PDF export reports a clear error rather than corrupting Japanese text if it is absent.
